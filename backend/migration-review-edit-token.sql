-- Міграція для ІСНУЮЧОЇ бази (виконати ОДИН РАЗ у Neon SQL Editor).
-- Додає можливість самостійно редагувати/видаляти свій відгук через
-- приватний токен — без реєстрації, без акаунтів.

ALTER TABLE reviews ADD COLUMN IF NOT EXISTS edit_token VARCHAR(64);
