-- Міграція для ІСНУЮЧОЇ бази (виконати ОДИН РАЗ у Neon SQL Editor).
-- Просте текстове поле для годин роботи — не окремо для кожного дня,
-- щоб не ускладнювати заповнення для вже наявних закладів.

ALTER TABLE venues ADD COLUMN IF NOT EXISTS working_hours VARCHAR(200);
